// access index number
// use indexOf method
void main(){
  List<int> number = [1,2,3];
  print(number);
  print(number.indexOf(1));
  // access index number
  print(number.indexOf(2));
  // indexOf method use the output index number
 print(number.indexOf(3));
// use String and access index number
List<String> name=['Roy', 'Rakib'];
print(name.indexOf('Roy'));
print(name.indexOf('Rakib'));
}